
const TemplatePage = ({ match }) => {
    const templateId = match.params.id;
    // Fetch template data based on the templateId
    
    return (
      <div>
        {/* Display template details */}
      </div>
    );
  };

  export default TemplatePage;

  